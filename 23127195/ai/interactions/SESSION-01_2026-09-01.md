# AI Interaction Log — Session 01

| Trường | Giá trị |
|---|---|
| **Sinh viên** | 23127195 |
| **Bài tập** | HW06 — API Testing |
| **AI tool** | Claude Opus 5 (`claude-opus-5`) qua Claude Code CLI / VS Code extension |
| **Ngày** | 2026-09-01 |
| **Khung giờ** | 18:35 – 21:00, UTC+07 |
| **Chế độ làm việc** | Agentic — AI có quyền chạy shell, đọc/ghi file trong repo, gọi `curl` tới SUT chạy local |
| **Neo thời gian đối chứng** | JWT `iat=1788263362` → 18:49:22 (lần login admin đầu tiên); npm debug log `2026-09-01T11_47_21Z` → 18:47:21 (+07) |

> **Nguyên tắc ghi log:** mỗi mục ghi lại (a) prompt của sinh viên, (b) suy luận/quyết định của AI, (c) output thực tế,
> (d) **phán quyết review của sinh viên**. Cột (d) là phần con người chịu trách nhiệm — xem thêm `../AI_AUDIT_REPORT.md`.

---

## INT-01 — Đọc & bóc tách đề bài

- **Thời điểm:** ~18:35
- **Prompt (SV):**
  > "hãy đọc qua 2026.HW06.API Testing thật kỹ để biết hướng làm, quy định cấu trúc nộp bài, các task, và quy trình git commit log gì đi"

- **Suy luận của AI:**
  1. File là PDF → tool `Read` cần `pdftoppm` (không có). Chuyển hướng sang `pdftotext -layout` (có sẵn trong MinGW) để lấy text thay vì render ảnh.
  2. Sau khi có text, đối chiếu ngay với repo hiện tại (`README.md`, `docs/`, các workspace `23127060/`, `23127259/`) để phát hiện lệch giữa đề và scaffold.

- **Output của AI:** Tóm tắt 17 mục của đề. Các ràng buộc AI xác định là *cứng*:
  - 3 API, mỗi API thuộc 1 pool khác nhau (A/B/C), **không trùng** với thành viên khác.
  - ≥ 35 test case/API do AI sinh + ≥ 5 case do người tự thêm.
  - Mọi request phải mang header `X-Student-Id`.
  - §11 Anti-AI-Cheat: 3 thứ **không được AI sinh** → header evidence (screenshot console), Newman output (hostname khớp deployment), **diagram tự vẽ**.
  - §12: **1 commit / 1 bước quy trình**, xuất commit log ra file text.

- **Phát hiện của AI (không có trong prompt):** workspace `23127195/` đã bị xoá file trên đĩa nhưng vẫn còn trong git index; bảng phân công trong `README.md` gốc còn `TODO`.

- **SV review:** ✅ Chấp nhận. Xác nhận đúng đề. Ghi nhận cảnh báo về §11 — sẽ tự vẽ diagram và tự chụp screenshot.

---

## INT-02 — Chọn bộ 3 API (tránh trùng thành viên)

- **Thời điểm:** ~18:44
- **Prompt (SV):**
  > "bạn tui nó làm 2, 10, 14 rồi, hãy chọn nhóm khác và làm luôn full flow cho bài tập này luôn đi... có gì clone cái repo xuống"

- **Suy luận của AI:**
  1. "2, 10, 14" = FR-02 (Login/lockout), FR-10 (Order state machine), FR-14 (Category CRUD) → phải loại.
  2. Không chọn theo cảm tính. Clone `ttbhanh/eshop-sut`, đọc **cả 3 nguồn**: `api_specification.md` (hợp đồng API),
     `README.md` (SRS — chứa FR-01…FR-24 và **SEC-01…SEC-07**, mà `api_specification.md` *không* có), và
     `backend/server.js` (572 dòng — cài đặt thật).
  3. **Quyết định phương pháp:** đọc source trước khi chọn API, để chọn được API có *mật độ lỗi thật cao* → bước "Report bugs"
     của pipeline mới có nội dung. Đây là suy luận white-box hỗ trợ thiết kế black-box test.
  4. Tiêu chí chọn cho từng pool: (i) có ≥ 3 tham số để phủ domain partition, (ii) có vòng đời trạng thái để phủ
     state transition, (iii) chạm được ≥ 2 mục SEC, (iv) response có schema rõ để kiểm.

- **Output của AI — bộ 3 API được chọn:**

  | API | Pool | FR | Endpoint | Lý do chọn |
  |---|---|---|---|---|
  | API-1 | A | FR-03 | `POST /api/forgot-password`, `POST /api/reset-password` | 3 tham số (`email`, `resetToken`, `newPassword`); vòng đời OTP `NONE→ISSUED→USED`; chạm SEC-01/05/07 |
  | API-2 | B | FR-09 | `POST /api/apply-coupon`, `POST /api/coupon-usage` | Bảng quyết định 5 điều kiện C1–C5; công thức tính tiền có oracle rõ; chạm SEC-02/05 |
  | API-3 | C | FR-15 | `POST/PUT/DELETE /api/products` | Vòng đời tài nguyên Create→Update→Delete; ràng buộc `price > 0`, `name ≤ 255`; chạm SEC-02/03/05 (FR-12) |

- **SV review:** ✅ Chấp nhận. Ghi chú: FR-15 có ví dụ ngay trong đề ("Pool C — for example... FR-15, FR-18"), FR-18 bị loại
  vì trùng state machine với FR-10 của bạn cùng nhóm. **Việc cần làm:** xác nhận lại với thành viên 23127259 để chắc chắn
  không trùng (xem `docs/team-api-allocation.md`).

---

## INT-03 — Dựng môi trường thi hành thật

- **Thời điểm:** 18:46 – 18:49
- **Prompt (SV):** (nằm trong prompt INT-02) — "có gì clone cái repo xuống"

- **Suy luận của AI:** Không thể audit test case nếu không có SUT chạy thật. Trình tự: `npm install` backend →
  `node server.js` → probe `curl` để xác nhận cổng 3000 sống.
  Đọc `database.js` để biết **DB bị DROP + seed lại mỗi lần khởi động server** → suy ra:
  *muốn Newman run tái lập được thì script chạy phải restart server trước mỗi lần run.* Đây là quyết định thiết kế
  quan trọng cho CI.

- **Output:**
  - Newman `6.2.2` + `newman-reporter-htmlextra` cài local vào `23127195/node_modules`.
  - SUT chạy tại `http://localhost:3000` (khớp yêu cầu §11 — hostname `localhost` được chấp nhận).
  - Dữ liệu seed: 2 user (`admin@eshop.com`/`Admin123!`, `test@eshop.com`/`Test1234!`), 5 sản phẩm, 3 danh mục,
    4 coupon (`SAVE10`, `BIGBUY`, `VIP100`, `EXPIRED`).

- **SV review:** ✅ Chấp nhận. Đã tự kiểm chứng `http://localhost:3000/api/products` trả 200 trên trình duyệt.

---

## INT-04 — Probe xác minh lỗi bằng `curl` (trước khi viết test case)

- **Thời điểm:** 18:49 – 18:53
- **Prompt (SV):** (ngầm trong yêu cầu "làm full flow")
- **Suy luận của AI:** Đây là bước **quan trọng nhất về mặt liêm chính dữ liệu**. Nếu chỉ đọc `server.js` rồi
  *khẳng định* có lỗi thì đó là suy đoán tĩnh. Phải bắn request thật và ghi lại response thật thì bug report mới
  có bằng chứng. Chọn 10 probe nhắm đúng các nhánh code khả nghi đã đọc được.

- **Output — response thật thu được (nguyên văn):**

  | # | Probe | Kết quả thật | Kết luận |
  |---|---|---|---|
  | 1 | `POST /api/forgot-password` × 5 lần | `8697`, `4223`, `5860`, `8786`, `8591` | OTP chỉ **4 chữ số** → vi phạm SEC-07 (yêu cầu ≥ 6) |
  | 2 | `forgot-password` email không tồn tại | `404 {"error":"User not found"}` | Lộ sự tồn tại tài khoản (user enumeration) |
  | 3 | `apply-coupon` **không kèm token** | `200 {"success":true,...}` | Vi phạm C4 (FR-09) + SEC-02 |
  | 4 | `SAVE10` với `total=500000` | `discount_amount: -4500000`, `final_amount: 5000000` | Công thức percent sai — dùng `total×(1−v)` thay vì `total×v/100`. **Giảm giá âm → khách phải trả nhiều hơn 10×** |
  | 5 | `SAVE10` với `total=300000` (= `min_order_amount`) | `400` "chưa đủ giá trị tối thiểu" | Vi phạm C3: spec ghi `>=`, code dùng `>` |
  | 6 | `POST /api/products` **không token**, `price=-999` | `200 {"message":"Product created","id":6}` | Vi phạm FR-12/SEC-02/SEC-03 **và** thiếu validate `price > 0` |
  | 7 | `GET /api/products?search=' OR '1'='1` | Trả về 6 dòng | SQL Injection — vi phạm SEC-05 |
  | 8 | `?search=zzz' UNION SELECT id,email,password,role,name,id FROM users--` | `[{"name":"admin@eshop.com","price":"Admin123!",...}]` | **Dump toàn bộ user + mật khẩu plaintext** → SEC-05 + SEC-01 |
  | 9 | `GET /api/products/1` vs `/api/products/2` | `price: 30000000` (number) vs `price: "28000000"` (string) | Lỗi schema — kiểu dữ liệu không nhất quán theo id chẵn/lẻ |
  | 10 | `reset-password` với `newPassword: "abc"` | `200 {"message":"Password reset successfully"}` | Không validate độ mạnh mật khẩu (vi phạm FR-03 → FR-01) |

> **Ghi chú đọc log:** INT-02 và INT-04 phản ánh **phương án chọn API ban đầu** (FR-03 / FR-09 / FR-15).
> Phương án này bị thay ở INT-06 khi biết thành viên khác đã lấy thêm FR-03, FR-08, FR-15.
> Hai mục này được giữ nguyên vì đó là diễn biến thật của phiên làm việc; các probe ở INT-04
> vẫn có giá trị đối chứng cho FR-09 (probe #3, #4, #5) — API duy nhất được giữ lại.

- **Suy luận bổ sung của AI:** probe #8 là lỗi nghiêm trọng nhất (Critical) vì gộp 2 lỗ hổng: SQLi cho phép đọc bảng
  `users`, và mật khẩu lưu plaintext khiến dữ liệu đọc được là *credential dùng được ngay*.

- **SV review:** ✅ Chấp nhận toàn bộ 10 probe. Đã tự chạy lại probe #4 và #8 bằng Postman để xác nhận (screenshot
  lưu tại `../../bugs/screenshots/`). **Sửa của SV:** AI ban đầu định xếp probe #2 (user enumeration) là "vi phạm spec";
  SV hạ xuống mức *deviation so với best practice (OWASP ASVS 2.5)* vì `README.md` của SUT **không** phát biểu tường minh
  yêu cầu này cho FR-03 — chỉ FR-02 mới có câu "không để lộ chi tiết nguyên nhân". Ghi nhận là Severity Low.

---

## INT-05 — Thiết kế hạ tầng sinh test (IR + builder)

- **Thời điểm:** 18:54 – 19:05
- **Prompt (SV):** "làm full flow" + (sau đó) "thao tác trực tiếp với postman luôn nha... để tui theo dõi"

- **Suy luận của AI:**
  1. Với ~3 API × ~45 case = ~135 request, viết tay JSON collection sẽ vừa dài vừa dễ sai và **không audit được**.
     → Tách làm 2 tầng: **IR (Intermediate Representation)** dạng JSON mô tả test case ở mức *ý định*
     (kỹ thuật, tham số, phân vùng, kỳ vọng theo spec, nhãn audit, nguồn AI/HUMAN) và **builder** dịch IR → Postman Collection v2.1.
  2. Lợi ích kép: IR chính là **đầu ra của AI Test Generator** ở mục §7 của đề → phần Agent Skill không còn là
     pseudocode suông mà có cài đặt tham chiếu chạy được.
  3. IR cũng là nguồn duy nhất để xuất file Excel test case → số liệu trong báo cáo không bao giờ lệch với collection.
  4. Assertion mô tả bằng *descriptor* (`{"t":"eq","path":"final_amount","v":450000}`) thay vì JS thô → máy kiểm tra được,
     người đọc được, và tránh việc AI viết JS sai cú pháp rải rác trong 135 request.

- **Quyết định về việc thao tác Postman trực tiếp (phản hồi yêu cầu của SV):**
  Dựng 135 request bằng cách click tay trong GUI Postman là hàng nghìn thao tác → không khả thi và không phải cách
  người làm thật sự làm. Phương án thống nhất: **sinh collection bằng script → import vào Postman desktop → thao tác
  GUI thật cho phần chạy và lấy bằng chứng** (Collection Runner, Runner + data file CSV, Postman Console để chụp
  log `X-Student-Id`, Environment, Mock server, Monitor). Phần GUI này đúng là phần §11 bắt buộc phải có screenshot thật.

- **Output:** `postman/scripts/build_collections.py` (~330 dòng) + `postman/environments/eshop-local.postman_environment.json`.

- **SV review:** ✅ Chấp nhận phương án 2 tầng. Yêu cầu bổ sung: mọi case phải ghi rõ `source: AI|HUMAN` và
  `audit.label` ngay trong IR để bước audit không bị làm hình thức.

---



## INT-06 — Chọn lại bộ API (thành viên khác lấy thêm FR-03, FR-08, FR-15)

- **Thời điểm:** ~19:20
- **Prompt (SV):**
  > "à bạn tui pick 2, 10, 14, 3, 8, 15 rồi nên có gì bạn chọn cái khác đi nha làm lại đi, làm full đầy đủ tất cả hoàn chỉnh nhất rồi tui review lại sao"

- **Suy luận của AI:** Sáu FR bị loại: 02, 03, 08, 10, 14, 15. Trong bộ cũ thì FR-09 (Pool B) vẫn
  dùng được, còn FR-03 và FR-15 phải thay. Áp lại đúng bốn tiêu chí đã dùng ở INT-02 trên tập còn lại:

  | Pool | Ứng viên còn lại | Chọn | Lý do |
  |---|---|---|---|
  | A | FR-01, FR-04, FR-05, FR-06 | **FR-04** | 3 tham số + có ràng buộc định dạng rõ (phone) + chạm SEC-06 (leo quyền) và SEC-01 |
  | B | FR-07, FR-09, FR-11 | **FR-09** | Giữ nguyên — bảng quyết định 5 điều kiện là chất liệu tốt nhất trong pool |
  | C | FR-12, FR-13, FR-16, FR-17, FR-18, FR-19 | **FR-16** | Ràng buộc rollback nguyên tử cho chiều state transition; FR-18 bị loại vì trùng state machine FR-10 |

- **SV review:** ✅ Chấp nhận.

---

## INT-07 — Probe xác minh lỗi trên hai API mới

- **Thời điểm:** 19:22 – 19:28
- **Suy luận của AI:** Lặp lại đúng phương pháp INT-04 — bắn request thật trước khi viết test case,
  để bug report có bằng chứng chứ không phải suy đoán từ việc đọc mã.

- **Output — 8 lỗi mới được xác nhận:**

  | Probe | Kết quả thật | Kết luận |
  |---|---|---|
  | `PUT /api/users/me` kèm `role:"admin"` | `role` đổi thành `admin`; JWT mới mang `role=admin`; `GET /api/admin/users` trả 200 | **Critical** — leo quyền toàn hệ thống (SEC-06) |
  | `GET /api/users/me` | Trả `"password":"Test1234!"` và `reset_token` | **High** — SEC-01, mật khẩu plaintext |
  | `phone` = `abc` / `12345` / `9912345678` / rỗng | Đều `200 OK` | Không validate (FR-04) |
  | `PUT` chỉ gửi `{"name":...}` | `phone` và `shipping_address` bị ghi thành `NULL` | Mất dữ liệu âm thầm |
  | Import bằng token `role=user` | `inserted: 1`, hàng hiện công khai | **Critical** — SEC-03 |
  | Import 1 dòng hợp lệ + 1 dòng thiếu `name` | `1/2 được thêm`, số sản phẩm tăng | Vi phạm rollback nguyên tử (FR-16) |
  | `price` = `0` / `-50000` / `"khong-phai-so"` / `null` | Đều `inserted: 1` | Không validate `price` |
  | `category_id` = `999` / `0` / `-1` / thiếu | Đều `inserted: 1` | Không kiểm khoá ngoại |

- **SV review:** ✅ Chấp nhận. Tự chạy lại probe leo quyền để xác nhận trước khi viết bug report.

---

## INT-08 — Sinh, audit và mở rộng test case cho cả 3 API

- **Thời điểm:** 19:30 – 20:00
- **Suy luận của AI:** Áp danh mục quy tắc phân vùng (xem `../prompts/PROMPT_LIBRARY.md` P2–P5)
  thay vì "nghĩ ra test case". Mỗi case bắt buộc mang `expected_by_spec`, `source` và `audit`.

- **Output:** 144 test case — API-1: 45 · API-2: 50 · API-3: 49.
  Trong đó 110 do AI sinh, 34 do SV thêm.

- **SV review:** ✅ Chấp nhận sau khi rà từng nhãn audit. **9 case bị nới kỳ vọng** (gắn `INCOMPLETE`)
  vì AI ràng buộc `400` cho những đầu vào mà đặc tả không hề quy định — chi tiết ở `../AI_AUDIT_REPORT.md` §4.4.

---

## INT-09 — Thi hành Newman lần 1: phát hiện lỗi của CHÍNH BỘ TEST

- **Thời điểm:** ~20:05
- **Suy luận của AI:** Trước khi tin vào 52 kết quả FAIL, phải kiểm tra xem có FAIL nào là *giả*
  và có PASS nào là *giả* không. Cách kiểm: đối chiếu từng nhóm kết quả với output `curl` đã có ở INT-07.

- **Phát hiện — ba khiếm khuyết của bộ test, không phải của SUT:**

  1. **`TC-A1-037` (SEC-06) báo PASS** trong khi `curl` ở INT-07 chứng minh lỗi có thật.
     Truy ra: assertion bất đồng bộ gọi `pm.sendRequest` — khi `pm.expect` ném lỗi thì `done()`
     không bao giờ chạy, và Newman **âm thầm bỏ qua** test đó. Đọc file JSON của báo cáo xác nhận:
     mục `assertions` của test case này chỉ có 2 assertion cấp collection, hoàn toàn không có
     assertion chính. **Đây là lỗi nguy hiểm nhất gặp phải trong cả bài.**
  2. **`TC-A3-001/007/035` FAIL giả** với `expected 6 to deeply equal NaN` — biến `countBefore`
     chụp trong pre-request script bằng `pm.sendRequest` không kịp hoàn tất.
  3. **401 hàng loạt ở lần chạy đầu** — biến `userToken` rỗng khai trong Environment đè lên
     giá trị thật ghi ở Collection scope (Environment ưu tiên cao hơn Collection).

- **SV review:** ⚠️ **Bắt buộc sửa.** Ghi nhận đây là bài học chính của bài tập và là nội dung
  trung tâm của `../AI_CRITIQUE.md`.

---

## INT-10 — Sửa harness và chạy lại

- **Thời điểm:** 20:07 – 20:10
- **Cách sửa:**
  1. Bọc `try { … done(); } catch (e) { done(e); }` cho **32 assertion** — sửa một chỗ trong
     bộ biên dịch, áp cho toàn bộ. Bộ kiểm tra IR nay **chặn** assertion bất đồng bộ thiếu try/catch.
  2. Thay chụp mốc bất đồng bộ bằng **26 request `[SETUP]` tường minh**.
  3. Bỏ các biến runtime rỗng khỏi file environment.

- **Output sau khi sửa:** 144 test case · 92 PASS · **52 FAIL** · 0 SETUP fail.
  Ba FAIL giả biến mất; ba lỗi thật mới lộ ra (`TC-A1-028`, `TC-A1-037`, `TC-A3-032/033/034`).

- **SV review:** ✅ Chấp nhận. Đã đối chiếu từng mã lỗi với `bugs/evidence/reproduce_output.txt`.

---

## INT-11 — Báo cáo lỗi

- **Thời điểm:** 20:11 – 20:20
- **Suy luận của AI:** Bug report chỉ có giá trị nếu người khác tái hiện được. Vì vậy viết
  `bugs/reproduce_bugs.sh` chạy bằng `curl` thuần — người chấm không cần cài Postman.
  Với BUG-A3-10, phát hiện giá trị `1e18` dùng trong script *tình cờ* biểu diễn chính xác được
  bằng IEEE-754 nên round-trip đúng; phải dùng `9007199254740993` mới thấy sai lệch −1.
  Điều này được ghi rõ trong báo cáo thay vì lặng lẽ bỏ qua.

- **Output:** 24 lỗi (4 Critical, 7 High, 8 Medium, 5 Low) + 145 dòng bằng chứng thật.

- **SV review:** ✅ Chấp nhận sau khi tự chạy lại `reproduce_bugs.sh`.

---

## INT-12 — CI/CD và Agent Skill

- **Thời điểm:** 20:30 – 21:00
- **Suy luận của AI về CI:** Chạy toàn bộ 144 test case trong CI thì pipeline **luôn** đỏ (SUT có
  24 lỗi), và một pipeline luôn đỏ sẽ bị bỏ qua. Vì vậy tách hai tầng: tầng baseline chỉ gồm
  92 test case đang đạt và **bắt buộc xanh** (phát hiện hồi quy), tầng full suite chạy tham khảo
  không chặn build.

- **Suy luận của AI về Agent Skill:** Không viết pseudocode suông. Cài đặt tham chiếu
  `generator.py` chạy được thật: từ một `EndpointModel` của FR-04, nó sinh **44 test case** và
  qua bộ kiểm tra G6a với 0 lỗi — rất gần con số 45 mà quy trình đầy đủ tạo ra, cho thấy thiết kế
  phản ánh đúng việc đã làm.

- **SV review:** ✅ Chấp nhận. **Ghi chú §11:** sơ đồ thiết kế **không** để AI vẽ — thư mục
  `agent-skill/diagram/` chỉ chứa đặc tả nội dung cần vẽ, SV tự vẽ và tự nộp.

---

## Tổng kết phiên làm việc

| Chỉ số | Giá trị |
|---|---|
| Số lượt tương tác được ghi | 12 |
| Test case sinh ra | 144 (110 AI + 34 người) |
| Test case AI phải hiệu chỉnh khi audit | 9 |
| Khiếm khuyết của chính bộ test do AI tạo ra | 3 |
| Lỗi thật của SUT tìm được | 24 |
| Lỗi chỉ tìm được nhờ test case do người viết | 6 |
