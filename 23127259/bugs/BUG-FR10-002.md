# [BUG-FR10-002] Canceled terminal order can be transitioned to delivered

## 1. Summary & Defect Overview
An order that has entered the terminal `canceled` state can be resurrected and transitioned to `delivered` by an Admin via `PUT /api/admin/orders/:id/status` with body `{"status":"delivered"}`. The SUT accepts the request with HTTP `200 OK` and mutates the persisted API-visible order status from `canceled` to `delivered`.

---

## 2. Requirement / Oracle Violation
- **Normative Source:** EShop SRS Document Section 4.10 (State Transition Constraints) & Level 2 FSM State Machine Definition.
- **Normative Rule:** `canceled` is a terminal sink state in the order state machine. Once an order is canceled, no further state transitions are permitted under any circumstances.
- **Expected Behavior:** `PUT /api/admin/orders/:id/status` with body `{"status":"delivered"}` on a `canceled` order must be rejected with a `4xx` Client Error (e.g., `400 Bad Request` or `422 Unprocessable Entity`), and the persisted API-visible order state must remain `canceled`.
- **Actual Behavior:** SUT returns HTTP `200 OK` and mutates the order status to `delivered`.

---

## 3. Severity & Impact
- **Severity:** **HIGH**
- **Impact:** Lifecycle integrity violation. Terminal orders (already refunded or discarded) can be improperly fulfilled and marked as delivered, corrupting audit trails, revenue reporting, and inventory tracking.

---

## 4. Preconditions & Test Environment
- **Host Under Test:** `http://localhost:3000`
- **Student Identifier:** `X-Student-Id: 23127259`
- **Authenticated Actors:**
  - System Admin: `admin@eshop.com` (`role = 'admin'`)
  - Customer (User A): `user@eshop.com` (`role = 'user'`)
- **Initial Fixture State:** Order placed by User A, transitioned by Admin to `canceled`. Initial state verified via authorized `GET /api/orders/:id` as `'canceled'`.

---

## 5. Steps to Reproduce
1. Authenticate as Admin (`role = 'admin'`) and obtain a valid JWT.
2. Authenticate as User A (`role = 'user'`) and obtain a valid JWT.
3. User A creates a fresh order via `POST /api/checkout` with header `X-Student-Id: 23127259`.
4. Admin transitions the order to `canceled` via `PUT /api/admin/orders/:id/status` body `{"status":"canceled"}`.
5. Verify initial order status via `GET /api/orders/:id` (returns `"status": "canceled"`).
6. As Admin, send mutation request targeting the terminal order:
   ```http
   PUT /api/admin/orders/:id/status HTTP/1.1
   Host: localhost:3000
   X-Student-Id: 23127259
   Authorization: Bearer <adminToken>
   Content-Type: application/json

   {
     "status": "delivered"
   }
   ```
7. Observe HTTP response status and body.
8. Execute authorized query `GET /api/orders/:id` to inspect persisted API-visible state.

---

## 6. Expected vs. Actual Result
- **Expected Result:** HTTP `4xx` Client Error; persisted API-visible state remains `'canceled'`.
- **Actual Result:**
  - HTTP Status: `200 OK`
  - Response Body: `{"message": "Order status updated", "status": "delivered"}`
  - Persisted state observed via authorized API GET: `'delivered'`

---

## 7. Reproducibility & Supporting Evidence
- **Reproducibility:** Observed in immutable historical Run03, corrected canonical Run04 (`FR10-AI-024`), and the targeted strict confirmation run.
- **Supporting Formal Test Cases:**
  - `FR10-AI-024`: Formal terminal state immutability test for `canceled -> delivered` (failed in corrected Run04).
- **Corrected Canonical Run:** `23127259/evidence/fr10/newman/FR10-run04.json` (SHA-256: `de73cc49094f7bdcea1db88f3f7f9c5369f973cf227d5b4efad192f6d1f81b99`).
- **Strict Evidence Run:** `23127259/evidence/fr10/confirmation/FR10-bug-evidence.json` (SHA-256: `25bf0214768921d1d902093e867bcacf8504f1b0e13252769b593b5c7ec9c1d2`).
- **Postman Desktop Screenshot:** [`BUG-FR10-002-postman-runner.png`](../evidence/fr10/bugs/BUG-FR10-002-postman-runner.png) (SHA-256: `919825d75d8d1117bd1076457f11d1a384d652b8263143b955018b2979271180`).
- **GitHub-renderable Screenshot:** ![BUG-FR10-002 native Postman Runner](https://raw.githubusercontent.com/thangak18/HW06/b069174/23127259/evidence/fr10/bugs/BUG-FR10-002-postman-runner.png)
- **Capture:** real Postman Desktop Collection Runner via native UI/Computer Use; no DOM rendering, image editing, or visible JWT.
- **GitHub Issue:** [#30](https://github.com/thangak18/HW06/issues/30)
