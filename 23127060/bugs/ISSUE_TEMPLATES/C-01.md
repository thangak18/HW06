[CRITICAL][C-01] `POST` / `PUT` / `DELETE /api/products` hoàn toàn không xác thực

> Báo cáo bởi Ninh Văn Khải — 23127060 | HW06 API Testing
> SUT: EShop, commit `85af3ba` | Môi trường: `http://localhost:3000`

## Mức độ

🔴 Critical

## Yêu cầu bị vi phạm

`SEC-02, SEC-03, FR-12` — theo `eshop-sut/README.md`

## Ảnh hưởng

SRS FR-12 liệt kê đích danh ba endpoint này trong nhóm bắt buộc phải có token JWT hợp lệ **và** `role = 'admin'`. Thực tế không có middleware nào cả. Một người hoàn toàn không đăng nhập có thể xóa sạch toàn bộ catalog sản phẩm, hoặc sửa giá mọi mặt hàng về 0. Đây là bug nghiêm trọng nhất của API-3.

## Bước tái hiện

```bash
curl -i -X POST 'http://localhost:3000/api/products' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060' \
     -d '{"name": "Khach vang lai 23127060", "price": 1, "description": "x", "imageUrl": "", "category_id": 1}'
```

## Kết quả thực tế

```http
HTTP/1.1 200
Content-Type: application/json; charset=utf-8

{"message":"Product created","id":13}
```

## Kết quả mong đợi

Theo `SEC-02, SEC-03, FR-12` trong đặc tả của hệ thống.

## Đề xuất sửa

Thêm `authenticateToken` và middleware kiểm `role === 'admin'` cho cả ba endpoint.

## Bằng chứng đầy đủ

Toàn bộ chuỗi request/response tái hiện: `bugs/evidence/C-01.md`

