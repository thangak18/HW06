[CRITICAL][B-01b] `checkout` chấp nhận `total_amount` âm

> Báo cáo bởi Ninh Văn Khải — 23127060 | HW06 API Testing
> SUT: EShop, commit `85af3ba` | Môi trường: `http://localhost:3000`

## Mức độ

🔴 Critical

## Yêu cầu bị vi phạm

`FR-08` — theo `eshop-sut/README.md`

## Ảnh hưởng

Trường hợp riêng của B-01 nhưng đáng chú ý riêng: đơn hàng có tổng tiền âm được tạo thành công. Nếu hệ thống có bước hoàn tiền hoặc tính doanh thu, số âm sẽ làm sai toàn bộ sổ sách.

## Bước tái hiện

```bash
curl -i -X POST 'http://localhost:3000/api/register' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060' \
     -d '{"name": "Victim", "email": "api.b01b.victim.23127060@test.local", "password": "Api1234!"}'
```

## Kết quả thực tế

```http
HTTP/1.1 200
Content-Type: application/json; charset=utf-8

{"message":"User registered successfully","id":12}
```

## Kết quả mong đợi

Theo `FR-08` trong đặc tả của hệ thống.

## Đề xuất sửa

Sau khi tự tính lại tổng tiền phía máy chủ, thêm ràng buộc `CHECK (total_amount > 0)` ở tầng CSDL.

## Bằng chứng đầy đủ

Toàn bộ chuỗi request/response tái hiện: `bugs/evidence/B-01b.md`

