[CRITICAL][A-07] Mật khẩu lưu plaintext và bị trả về trong response của `login` / `users/me`

> Báo cáo bởi Ninh Văn Khải — 23127060 | HW06 API Testing
> SUT: EShop, commit `85af3ba` | Môi trường: `http://localhost:3000`

## Mức độ

🔴 Critical

## Yêu cầu bị vi phạm

`SEC-01` — theo `eshop-sut/README.md`

## Ảnh hưởng

Cột `password` lưu nguyên văn. `SELECT *` rồi `res.json(user)` đưa cả `password` lẫn `reset_token` ra ngoài. Bất kỳ ai xem được một response login (log, proxy, cache trình duyệt) đều có mật khẩu thật. Vì người dùng thường dùng lại mật khẩu, thiệt hại vượt ra ngoài hệ thống này.

## Kết quả mong đợi

Theo `SEC-01` trong đặc tả của hệ thống.

## Đề xuất sửa

Băm mật khẩu bằng `bcrypt` khi ghi. Khi đọc, chọn đúng cột cần dùng thay vì `SELECT *`, hoặc loại bỏ `password` và `reset_token` trước khi trả về.

## Bằng chứng đầy đủ

Toàn bộ chuỗi request/response tái hiện: `bugs/evidence/A-07.md`

