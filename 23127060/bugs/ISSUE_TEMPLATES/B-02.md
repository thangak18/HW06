[CRITICAL][B-02] `GET /api/orders/:id` thiếu hẳn xác thực - IDOR

> Báo cáo bởi Ninh Văn Khải — 23127060 | HW06 API Testing
> SUT: EShop, commit `85af3ba` | Môi trường: `http://localhost:3000`

## Mức độ

🔴 Critical

## Yêu cầu bị vi phạm

`SEC-02` — theo `eshop-sut/README.md`

## Ảnh hưởng

Endpoint không có middleware `authenticateToken`. Bất kỳ ai duyệt lần lượt id từ 1 đều đọc được toàn bộ đơn hàng của mọi người dùng: địa chỉ giao hàng, tổng tiền, trạng thái. Đây là lộ lọt dữ liệu cá nhân trên diện rộng.

## Bước tái hiện

```bash
curl -i -X POST 'http://localhost:3000/api/register' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060' \
     -d '{"name": "Victim", "email": "api.b02.victim.23127060@test.local", "password": "Api1234!"}'
```

## Kết quả thực tế

```http
HTTP/1.1 200
Content-Type: application/json; charset=utf-8

{"message":"User registered successfully","id":13}
```

## Kết quả mong đợi

Theo `SEC-02` trong đặc tả của hệ thống.

## Đề xuất sửa

Thêm `authenticateToken` vào endpoint, và thêm điều kiện `AND user_id = ?` vào câu truy vấn để người dùng chỉ đọc được đơn của chính mình.

## Bằng chứng đầy đủ

Toàn bộ chuỗi request/response tái hiện: `bugs/evidence/B-02.md`

