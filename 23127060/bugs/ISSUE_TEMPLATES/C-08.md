[MEDIUM][C-08] `DELETE /api/products/:id` với id không tồn tại vẫn trả `200 Product deleted`

> Báo cáo bởi Ninh Văn Khải — 23127060 | HW06 API Testing
> SUT: EShop, commit `85af3ba` | Môi trường: `http://localhost:3000`

## Mức độ

🟡 Medium

## Yêu cầu bị vi phạm

`FR-15` — theo `eshop-sut/README.md`

## Ảnh hưởng

Cùng nguyên nhân với C-07.

## Bước tái hiện

```bash
curl -i -X DELETE 'http://localhost:3000/api/products/999999' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060'
```

## Kết quả thực tế

```http
HTTP/1.1 200
Content-Type: application/json; charset=utf-8

{"message":"Product deleted"}
```

## Kết quả mong đợi

Theo `FR-15` trong đặc tả của hệ thống.

## Đề xuất sửa

Kiểm `this.changes` trước khi trả về thành công.

## Bằng chứng đầy đủ

Toàn bộ chuỗi request/response tái hiện: `bugs/evidence/C-08.md`

