[CRITICAL][B-07] `apply-coupon` không xác thực; bỏ `user_id` là bỏ qua toàn bộ kiểm tra hạn mức

> Báo cáo bởi Ninh Văn Khải — 23127060 | HW06 API Testing
> SUT: EShop, commit `85af3ba` | Môi trường: `http://localhost:3000`

## Mức độ

🔴 Critical

## Yêu cầu bị vi phạm

`SEC-02, FR-09` — theo `eshop-sut/README.md`

## Ảnh hưởng

Endpoint không có `authenticateToken` và lấy `user_id` từ body. Nghiêm trọng hơn: phép kiểm hạn mức nằm trong nhánh `if (user_id)`, nên **không gửi** trường này sẽ đi vào nhánh `else` và áp mã mà không đếm lượt nào cả. Đây là nghịch lý "bỏ bớt dữ liệu để được nhiều quyền hơn": mã giới hạn một lượt mỗi người trở thành dùng được vô hạn.

## Bước tái hiện

```bash
curl -i -X POST 'http://localhost:3000/api/apply-coupon' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060' \
     -d '{"code": "VIP100", "total_amount": 500000}'
```

## Kết quả thực tế

```http
HTTP/1.1 200
Content-Type: application/json; charset=utf-8

{"success":true,"coupon_id":3,"discount_amount":100000,"final_amount":400000,"message":"Áp dụng thành công! Giảm 100,000 ₫"}
```

## Kết quả mong đợi

Theo `SEC-02, FR-09` trong đặc tả của hệ thống.

## Đề xuất sửa

Thêm `authenticateToken` và lấy `user_id` từ `req.user.id`, không bao giờ từ body. Bỏ hoàn toàn nhánh `else`.

## Bằng chứng đầy đủ

Toàn bộ chuỗi request/response tái hiện: `bugs/evidence/B-07.md`

