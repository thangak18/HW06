[MEDIUM][B-11] `POST /api/coupon-usage` ghi nhận lượt dùng cho `coupon_id` không tồn tại

> Báo cáo bởi Ninh Văn Khải — 23127060 | HW06 API Testing
> SUT: EShop, commit `85af3ba` | Môi trường: `http://localhost:3000`

## Mức độ

🟡 Medium

## Yêu cầu bị vi phạm

`FR-09` — theo `eshop-sut/README.md`

## Ảnh hưởng

Không kiểm tra mã giảm giá có tồn tại không, cũng không gắn với đơn hàng nào. Kẻ tấn công tạo được bản ghi rác, hoặc chèn lượt dùng giả cho tài khoản người khác để họ không dùng được mã nữa.

## Bước tái hiện

```bash
curl -i -X POST 'http://localhost:3000/api/register' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060' \
     -d '{"name": "Victim", "email": "api.b11.victim.23127060@test.local", "password": "Api1234!"}'
```

## Kết quả thực tế

```http
HTTP/1.1 200
Content-Type: application/json; charset=utf-8

{"message":"User registered successfully","id":18}
```

## Kết quả mong đợi

Theo `FR-09` trong đặc tả của hệ thống.

## Đề xuất sửa

Kiểm `coupon_id` tồn tại và gắn bản ghi với `order_id` thật. Tốt nhất là ghi nhận lượt dùng ngay trong giao dịch thanh toán thay vì để client gọi một endpoint riêng.

## Bằng chứng đầy đủ

Toàn bộ chuỗi request/response tái hiện: `bugs/evidence/B-11.md`

