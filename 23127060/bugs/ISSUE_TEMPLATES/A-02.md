[HIGH][A-02] OTP chỉ có 4 chữ số trong khi đặc tả đòi tối thiểu 6

> Báo cáo bởi Ninh Văn Khải — 23127060 | HW06 API Testing
> SUT: EShop, commit `85af3ba` | Môi trường: `http://localhost:3000`

## Mức độ

🟠 High

## Yêu cầu bị vi phạm

`SEC-07, FR-03` — theo `eshop-sut/README.md`

## Ảnh hưởng

Không gian mã chỉ 9000 giá trị (1000-9999). Không có giới hạn số lần thử nên dò hết toàn bộ không gian là khả thi. Nghiêm trọng hơn: với 4 chữ số, chỉ cần khoảng 100 người cùng đang chờ reset là xác suất có hai người trùng mã vượt 40% (nghịch lý ngày sinh) - khi đó điều kiện `email AND reset_token` không còn bảo vệ được ai.

## Bước tái hiện

```bash
curl -i -X POST 'http://localhost:3000/api/forgot-password' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060' \
     -d '{"email": "api.a02.victim.23127060@test.local"}'
```

## Kết quả thực tế

```http
HTTP/1.1 200
Content-Type: application/json; charset=utf-8

{"message":"Mã đặt lại mật khẩu đã được tạo","resetToken":"2269"}
```

## Kết quả mong đợi

Theo `SEC-07, FR-03` trong đặc tả của hệ thống.

## Đề xuất sửa

`Math.floor(100000 + Math.random() * 900000)` cho 6 chữ số, và tốt hơn là dùng `crypto.randomInt` thay vì `Math.random` (không an toàn về mặt mật mã).

## Bằng chứng đầy đủ

Toàn bộ chuỗi request/response tái hiện: `bugs/evidence/A-02.md`

