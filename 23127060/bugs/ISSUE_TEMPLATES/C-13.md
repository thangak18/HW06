[CRITICAL][C-13] Một sản phẩm có `price = null` làm SẬP HẲN backend khi đọc lại (từ chối dịch vụ)

> Báo cáo bởi Ninh Văn Khải — 23127060 | HW06 API Testing
> SUT: EShop, commit `85af3ba` | Môi trường: `http://localhost:3000`

## Mức độ

🔴 Critical

## Yêu cầu bị vi phạm

`FR-15` — theo `eshop-sut/README.md`

## Ảnh hưởng

Đây là bug nguy hiểm nhất em tìm được, và nó là **hệ quả dây chuyền của hai bug khác**. C-09 cho phép một lệnh `PUT` thiếu trường ghi đè `price` thành `null`. Sau đó, C-05 chạy `row.price.toString()` trên giá trị `null` khi id là số chẵn. `TypeError` ném ra trong callback của `sqlite3` không được ai bắt, Node thoát hẳn, **toàn bộ API ngưng phục vụ**. Trong lần chạy thử nghiệm, request tiếp theo trả về `Connection refused` và mọi kịch bản sau đó không chạy được nữa. Một người không đăng nhập có thể hạ gục toàn bộ hệ thống bằng **hai** request (C-01 cho phép gọi `PUT` mà không cần token). Không bug nào trong ba bug thành phần tự nó gây sập; chỉ tổ hợp của chúng mới gây.

## Kết quả mong đợi

Theo `FR-15` trong đặc tả của hệ thống.

## Đề xuất sửa

Sửa cả ba: (1) C-01 thêm xác thực; (2) C-09 chỉ cập nhật trường được gửi; (3) C-05 xóa dòng ép kiểu. Ngoài ra bắt buộc phải có `process.on('uncaughtException')` và một tầng xử lý lỗi cho mọi callback của tầng CSDL, để một bản ghi hỏng không thể hạ được cả tiến trình.

## Bằng chứng đầy đủ

Toàn bộ chuỗi request/response tái hiện: `bugs/evidence/C-13.md`

