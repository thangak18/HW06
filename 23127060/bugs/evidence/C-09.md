# Bằng chứng tái hiện bug C-09

**PUT không hỗ trợ cập nhật một phần: trường không gửi bị ghi đè thành null**

Thứ tự 3 bước dưới đây được chạy tự động bằng
`agent-skill/eshop-api-23127060/scripts/capture_bug_evidence.py` tren
`http://localhost:3000` luc 2026-09-02 23:57:40 +0700.
Bước CUỐI CÙNG là bước phơi bày bug; các bước trước chỉ dùng để chuẩn bị trạng thái.

## Bước 1/3 (chuẩn bị)

```bash
curl -i -X POST 'http://localhost:3000/api/products' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060' \
     -d '{"name": "Day du 23127060", "price": 150000, "description": "Mo ta day du", "imageUrl": "https://e.com/a.png", "category_id": 2}'
```

```http
HTTP/1.1 200
Content-Type: application/json; charset=utf-8

{"message":"Product created","id":16}
```

## Bước 2/3 (chuẩn bị)

```bash
curl -i -X PUT 'http://localhost:3000/api/products/16' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060' \
     -d '{"name": "Chi doi ten 23127060"}'
```

```http
HTTP/1.1 200
Content-Type: application/json; charset=utf-8

{"message":"Product updated"}
```

## **BƯỚC 3/3 — PHƠI BÀY BUG**

```bash
curl -i -X GET 'http://localhost:3000/api/products/16' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060'
```

```http
HTTP/1.1 0
Content-Type: (không có)

Remote end closed connection without response
```

