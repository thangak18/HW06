# Bằng chứng tái hiện bug C-13

**Một sản phẩm có price = null làm SẬP HẲN backend khi đọc lại (từ chối dịch vụ)**

Thứ tự 4 bước dưới đây được chạy tự động bằng
`agent-skill/eshop-api-23127060/scripts/capture_bug_evidence.py` tren
`http://localhost:3000` luc 2026-09-02 23:57:45 +0700.
Bước CUỐI CÙNG là bước phơi bày bug; các bước trước chỉ dùng để chuẩn bị trạng thái.

## Bước 1/4 (chuẩn bị)

```bash
curl -i -X GET 'http://localhost:3000/api/products/2' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060'
```

```http
HTTP/1.1 200
Content-Type: application/json; charset=utf-8

{"id":2,"name":"Samsung Galaxy S24 Ultra","price":"28000000","description":"Màn hình hiển thị xuất sắc, camera siêu zoom","imageUrl":"https://placehold.co/300x300/png?text=Samsung+S24","category_id":1}
```

## Bước 2/4 (chuẩn bị)

```bash
curl -i -X PUT 'http://localhost:3000/api/products/2' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060' \
     -d '{"name": "Chi con ten 23127060"}'
```

```http
HTTP/1.1 200
Content-Type: application/json; charset=utf-8

{"message":"Product updated"}
```

## Bước 3/4 (chuẩn bị)

```bash
curl -i -X GET 'http://localhost:3000/api/products/2' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060'
```

```http
HTTP/1.1 0
Content-Type: (không có)

Remote end closed connection without response
```

## **BƯỚC 4/4 — PHƠI BÀY BUG**

```bash
curl -i -X GET 'http://localhost:3000/api/products' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060'
```

```http
HTTP/1.1 0
Content-Type: (không có)

<urlopen error [Errno 111] Connection refused>
```

