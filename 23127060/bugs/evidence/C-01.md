# Bằng chứng tái hiện bug C-01

**POST/PUT/DELETE /api/products hoàn toàn không xác thực**

Thứ tự 3 bước dưới đây được chạy tự động bằng
`agent-skill/eshop-api-23127060/scripts/capture_bug_evidence.py` tren
`http://localhost:3000` luc 2026-09-02 23:57:40 +0700.
Bước CUỐI CÙNG là bước phơi bày bug; các bước trước chỉ dùng để chuẩn bị trạng thái.

## Bước 1/3 (chuẩn bị)

```bash
curl -i -X POST 'http://localhost:3000/api/products' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060' \
     -d '{"name": "Khach vang lai 23127060", "price": 1, "description": "x", "imageUrl": "", "category_id": 1}'
```

```http
HTTP/1.1 200
Content-Type: application/json; charset=utf-8

{"message":"Product created","id":13}
```

## Bước 2/3 (chuẩn bị)

```bash
curl -i -X PUT 'http://localhost:3000/api/products/13' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060' \
     -d '{"name": "Bi sua khong can token", "price": 2, "description": "x", "imageUrl": "", "category_id": 1}'
```

```http
HTTP/1.1 200
Content-Type: application/json; charset=utf-8

{"message":"Product updated"}
```

## **BƯỚC 3/3 — PHƠI BÀY BUG**

```bash
curl -i -X DELETE 'http://localhost:3000/api/products/13' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060'
```

```http
HTTP/1.1 200
Content-Type: application/json; charset=utf-8

{"message":"Product deleted"}
```

