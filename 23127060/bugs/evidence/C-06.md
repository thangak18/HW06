# Bằng chứng tái hiện bug C-06

**POST /api/products không validate gì: giá âm, giá là chuỗi, tên null đều được chấp nhận**

Thứ tự 2 bước dưới đây được chạy tự động bằng
`agent-skill/eshop-api-23127060/scripts/capture_bug_evidence.py` tren
`http://localhost:3000` luc 2026-09-02 23:57:40 +0700.
Bước CUỐI CÙNG là bước phơi bày bug; các bước trước chỉ dùng để chuẩn bị trạng thái.

## Bước 1/2 (chuẩn bị)

```bash
curl -i -X POST 'http://localhost:3000/api/products' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060' \
     -d '{"name": "Gia am 23127060", "price": -100, "description": "x", "imageUrl": "", "category_id": 1}'
```

```http
HTTP/1.1 200
Content-Type: application/json; charset=utf-8

{"message":"Product created","id":14}
```

## **BƯỚC 2/2 — PHƠI BÀY BUG**

```bash
curl -i -X POST 'http://localhost:3000/api/products' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060' \
     -d '{"name": null, "price": "abc", "description": "x", "imageUrl": "", "category_id": 1}'
```

```http
HTTP/1.1 200
Content-Type: application/json; charset=utf-8

{"message":"Product created","id":15}
```

