# Bằng chứng tái hiện bug A-09

**Bộ đếm đăng nhập sai cộng +2 mỗi lần nên khóa ngay ở lần sai thứ HAI**

Thứ tự 4 bước dưới đây được chạy tự động bằng
`agent-skill/eshop-api-23127060/scripts/capture_bug_evidence.py` tren
`http://localhost:3000` luc 2026-09-02 23:57:39 +0700.
Bước CUỐI CÙNG là bước phơi bày bug; các bước trước chỉ dùng để chuẩn bị trạng thái.

## Bước 1/4 (chuẩn bị)

```bash
curl -i -X POST 'http://localhost:3000/api/register' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060' \
     -d '{"name": "Victim", "email": "api.a09.victim.23127060@test.local", "password": "Api1234!"}'
```

```http
HTTP/1.1 200
Content-Type: application/json; charset=utf-8

{"message":"User registered successfully","id":10}
```

## Bước 2/4 (chuẩn bị)

```bash
curl -i -X POST 'http://localhost:3000/api/login' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060' \
     -d '{"email": "api.a09.victim.23127060@test.local", "password": "SaiRoi1!"}'
```

```http
HTTP/1.1 401
Content-Type: application/json; charset=utf-8

{"error":"Invalid email or password"}
```

## Bước 3/4 (chuẩn bị)

```bash
curl -i -X POST 'http://localhost:3000/api/login' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060' \
     -d '{"email": "api.a09.victim.23127060@test.local", "password": "SaiRoi2!"}'
```

```http
HTTP/1.1 401
Content-Type: application/json; charset=utf-8

{"error":"Invalid email or password"}
```

## **BƯỚC 4/4 — PHƠI BÀY BUG**

```bash
curl -i -X POST 'http://localhost:3000/api/login' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060' \
     -d '{"email": "api.a09.victim.23127060@test.local", "password": "Api1234!"}'
```

```http
HTTP/1.1 403
Content-Type: application/json; charset=utf-8

{"error":"Tài khoản đã bị khóa. Vui lòng thử lại sau."}
```

