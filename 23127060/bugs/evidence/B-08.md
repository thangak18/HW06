# Bằng chứng tái hiện bug B-08

**Kiểm tra hạn sử dụng nằm trong nhánh total > min nên thông báo lỗi sai nguyên nhân**

Thứ tự 1 bước dưới đây được chạy tự động bằng
`agent-skill/eshop-api-23127060/scripts/capture_bug_evidence.py` tren
`http://localhost:3000` luc 2026-09-02 23:57:40 +0700.
Bước CUỐI CÙNG là bước phơi bày bug; các bước trước chỉ dùng để chuẩn bị trạng thái.

## **BƯỚC 1/1 — PHƠI BÀY BUG**

```bash
curl -i -X POST 'http://localhost:3000/api/apply-coupon' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060' \
     -d '{"code": "EXPIRED", "total_amount": 50000, "user_id": 1}'
```

```http
HTTP/1.1 400
Content-Type: application/json; charset=utf-8

{"error":"Đơn hàng chưa đủ giá trị tối thiểu 100,000 ₫ để áp dụng mã này"}
```

