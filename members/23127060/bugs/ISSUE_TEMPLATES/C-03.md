[HIGH][C-03] Loi SQL tra ve HTML kem thong diep cua tang CSDL

> Bao cao boi Ninh Van Khai — 23127060 | HW06 API Testing
> SUT: EShop, commit `85af3ba` | Moi truong: `http://localhost:3000`

## Muc do

🟠 High

## Yeu cau bi vi pham

`SEC-05` — theo `eshop-sut/README.md`

## Anh huong

Khi truy van loi, may chu tra `res.status(500).send('<h1>Database Error</h1><p>' + err.message + '</p>')` voi `Content-Type: text/html`. Hai hau qua: lo cau truc CSDL cho ke tan cong (giup tinh chinh payload SQLi), va pha vo hop dong JSON khien client goi `response.json()` bi nem loi.

## Buoc tai hien

```bash
curl -i -X GET 'http://localhost:3000/api/products?search=%27' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060'
```

## Ket qua thuc te

```http
HTTP/1.1 500
Content-Type: text/html; charset=utf-8

<h1>Database Error</h1><p>SQLITE_ERROR: unrecognized token: "'"</p>
```

## Ket qua mong doi

Theo `SEC-05` trong dac ta cua he thong.

## De xuat sua

Tra ve `res.status(500).json({ error: 'Internal server error' })`. Ghi `err.message` vao log may chu, khong gui cho client.

## Bang chung day du

Toan bo chuoi request/response tai hien: `bugs/evidence/C-03.md`

