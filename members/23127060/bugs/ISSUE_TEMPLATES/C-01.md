[CRITICAL][C-01] `POST` / `PUT` / `DELETE /api/products` hoan toan khong xac thuc

> Bao cao boi Ninh Van Khai — 23127060 | HW06 API Testing
> SUT: EShop, commit `85af3ba` | Moi truong: `http://localhost:3000`

## Muc do

🔴 Critical

## Yeu cau bi vi pham

`SEC-02, SEC-03, FR-12` — theo `eshop-sut/README.md`

## Anh huong

SRS FR-12 liet ke dich danh ba endpoint nay trong nhom bat buoc phai co token JWT hop le **va** `role = 'admin'`. Thuc te khong co middleware nao ca. Mot nguoi hoan toan khong dang nhap co the xoa sach toan bo catalog san pham, hoac sua gia moi mat hang ve 0. Day la bug nghiem trong nhat cua API-3.

## Buoc tai hien

```bash
curl -i -X DELETE 'http://localhost:3000/api/products/14' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060'
```

## Ket qua thuc te

```http
HTTP/1.1 200
Content-Type: application/json; charset=utf-8

{"message":"Product deleted"}
```

## Ket qua mong doi

Theo `SEC-02, SEC-03, FR-12` trong dac ta cua he thong.

## De xuat sua

Them `authenticateToken` va middleware kiem `role === 'admin'` cho ca ba endpoint.

## Bang chung day du

Toan bo chuoi request/response tai hien: `bugs/evidence/C-01.md`

