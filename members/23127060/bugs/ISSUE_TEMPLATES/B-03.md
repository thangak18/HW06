[CRITICAL][B-03] `PUT /api/admin/orders/:id/status` khong kiem tra `role`

> Bao cao boi Ninh Van Khai — 23127060 | HW06 API Testing
> SUT: EShop, commit `85af3ba` | Moi truong: `http://localhost:3000`

## Muc do

🔴 Critical

## Yeu cau bi vi pham

`SEC-03, FR-12` — theo `eshop-sut/README.md`

## Anh huong

Endpoint co `authenticateToken` nhung khong he doc `req.user.role`. Bat ky nguoi dung dang nhap nao cung doi duoc trang thai don hang cua nguoi khac - vi du danh dau don cua nguoi khac la da giao de chan viec huy don.

## Buoc tai hien

```bash
curl -i -X PUT 'http://localhost:3000/api/admin/orders/12/status' \
     -H 'Content-Type: application/json' \
     -H 'X-Student-Id: 23127060' \
     -H 'Authorization: Bearer <token>' \
     -d '{"status": "confirmed"}'
```

## Ket qua thuc te

```http
HTTP/1.1 200
Content-Type: application/json; charset=utf-8

{"message":"Order status updated"}
```

## Ket qua mong doi

Theo `SEC-03, FR-12` trong dac ta cua he thong.

## De xuat sua

Them middleware kiem quyen: `if (req.user.role !== 'admin') return res.status(403).json(...)`. Ap cho toan bo nhom duong dan `/api/admin/*`.

## Bang chung day du

Toan bo chuoi request/response tai hien: `bugs/evidence/B-03.md`

